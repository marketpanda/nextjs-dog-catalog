/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.dog.ceo', 'clipart-library.com', 'res.cloudinary.com'] 
    }
    
        // loader: 'cloudinary',
        // path: 'https://res.cloudinary.com/dd7ticf6t'
}

module.exports = nextConfig
